# Domain Adapters

API adapters for different search domains. Each adapter implements the common search interface for a specific platform (e.g., YouTube, SoundCloud, GitHub, etc.), normalizing results into the breakout scoring pipeline.
