YOUTUBE_API_KEY=your_key_here
# GitHub uses `gh auth token` automatically — no key needed
# Semantic Scholar — no key needed for basic use
# Hacker News — no auth needed
# Medium — no auth needed (uses RSS/scraping)
